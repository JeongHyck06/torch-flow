# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
build/
dist/

# Node
node_modules/
web/dist/

# 프론트 빌드 산출물 — `npm run build`가 만든다. wheel 빌드 시 생성한다.
src/torchflow/hub/static/

# 로컬 상태 (--state-dir 기본값): 캐시·journal·layout·registry
.torchflow/
runs/
# 내려받은 데이터셋 (MNIST 등)
data/
# 학습 중 hparam 변경 기록. 사용자 프로젝트에서는 커밋 대상이지만
# 이 저장소에서 나오는 것은 예제를 돌린 흔적이다.
conf/

# 빌드 산출물
web/tsconfig.tsbuildinfo

# 기획 문서 (별도 관리)
docs/

# 도구 산출물
graphify-out/
CLAUDE.md
.claude/

# OS
.DS_Store
