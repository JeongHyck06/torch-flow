"""생성 코드에 그대로 실리는 런타임 헬퍼.

이 패키지의 모듈은 torchflow에 의존하지 않는다. codegen이 소스를 읽어
``utils.py``에 인라인하고, L1 커널은 같은 파일을 import해서 쓴다 - 두 경로가
바이트 단위로 같은 코드를 돌리는 것이 §10.4의 "step-0 state_dict 바이트 동일성"
검사가 성립하는 이유다.
"""
